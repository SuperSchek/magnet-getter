const puppeteer = require("puppeteer");

class Crawler {
  constructor() {}

  getQueryResults(query, agent) {}

  /**
   *
   * @param {number} url
   * @param {number} querySelector
   */
  getMagnetLink(url, querySelector) {}
}

module.exports = Crawler;
